SUBROUTINE STRING.LEFT(SOMETHING,HOW.MANY,LEFT.CHARS)
   TOTAL.COUNT=LEN(SOMETHING)
   IF HOW.MANY>TOTAL.COUNT THEN CRT "STRING.LEFT ERROR: You requested more characters than are available!"
   LEFT.CHARS = SOMETHING[1,HOW.MANY]
RETURN