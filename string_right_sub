SUBROUTINE STRING.RIGHT(SOMETHING,HOW_MANY,RIGHT_CHARS)
* DESCRIPTION: Returns a specified number of characters from the end of a string
   TOTAL.COUNT = LEN(SOMETHING)
   IF HOW_MANY > TOTAL.COUNT THEN CRT "STRING.RIGHT ERROR: You requested more characters than are available!"
   IF HOW_MANY > 1 THEN TOTAL.COUNT -= (HOW_MANY - 1)
   RIGHT_CHARS = SOMETHING[TOTAL.COUNT,HOW_MANY]
RETURN