SUBROUTINE STRING.WRAP.PREPEND(SOMETHING,WITH_THIS,BEFORE_WHAT)    
* DESCRIPTION FIRST WRAP SOMETHING THEN PREPEND IT TO BEFORE_WHAT
*  Example use case: wrap the key with double quotes and prepend the key to a colon
   CALL STRING.WRAP(SOMETHING,WITH_THIS,WRAPPED_STRING)            ;* WRAP SOMETHING
   CALL STRING.PREPEND(WRAPPED_STRING,BEFORE_WHAT)                 ;* PREPEND THE WRAPPED.STRING with whatever is passed in BEFORE_WHAT
RETURN