SUBROUTINE STRING.WRAP.PREPEND(SOMETHING,WITH_THIS,BEFORE.WHAT)    ;* SAYS FIRST WRAP SOMETHING THEN PREPEND IT TO SOMETHING ELSE
*  Example use case: wrap the key with double quotes and prepend the key to a colon
   CALL STRING.WRAP(SOMETHING,WITH_THIS,WRAPPED.STRING)            ;* WRAP SOMETHING
   CALL STRING.PREPEND(WRAPPED.STRING,BEFORE.WHAT)                 ;* PREPEND THE WRAPPED.STRING with whatever is passed in BEFORE.WHAT
RETURN